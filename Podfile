# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'
workspace 'DawnPlusWorkSpace'

target 'TrafficAlarmClock' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TrafficAlarmClock
  pod 'DZNEmptyDataSet'
  pod 'MGSwipeTableCell'

  target 'TrafficAlarmClockTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TrafficAlarmClockUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
